---
title: prompts
status: active
hue: 110
desc: Grouping home for the `surface: system` plugins — the always-on prompt contracts materialized into the agent's contract block. A shelf, not a surface — routing stays field-driven.
---
# prompts

The `surface: system` plugins live here: the plugins whose body IS prose an agent must always carry —
materialized (in name order) into the `<!-- spexcode -->` contract block — rather than a verb it invokes.
Grouping them keeps `.plugins/` legible at a glance: prompt contracts on this shelf, invocable surfaces
(command/skill/agent) as flat children beside it.

This node is a **shelf, not a surface**: it declares no `surface` field and gathers nothing itself.
Discovery is recursive and field-driven, so residents plug in exactly as they would at the root —
a resident may itself be a grouping parent (`core` carries the `surface: hook` children).
