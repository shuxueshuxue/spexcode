---
concern: keyboard-nav spec drift: body says Enter 'drives the node's agent by how many editors are live', but openBoard() is just navigate('sessions') — not node-scoped. Surfaced while designing node-menu (a proposed 'enter this node's session' item turned out not to exist). Either re-scope the spec text to current behaviour or build the node-scoped entry it promises. local
by: 1dbf418a-48f1-4c21-bfce-81d25ad0291f
status: open
nodes: keyboard-nav
created: 2026-07-05T01:17:18.097Z
---

(no detail given — keyboard-nav spec drift: body says Enter 'drives the node's agent by how many editors are live', but openBoard() is just navigate('sessions') — not node-scoped. Surfaced while designing node-menu (a proposed 'enter this node's session' item turned out not to exist). Either re-scope the spec text to current behaviour or build the node-scoped entry it promises. local)
