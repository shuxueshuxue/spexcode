# dependencies & build output
node_modules/
dist/
.vite/

# SpexCode worktrees live here — they are separate checkouts, never committed
.worktrees/

# per-worktree session linker — ephemeral runtime state, not spec data
.session

# harness state
.claude/

# os / editor
.DS_Store
*.log
