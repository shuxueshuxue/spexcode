{
  "name": "@spexcode/spec-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Host-agnostic forge projection — projects pending spec nodes out as forge issues. git/.spec stays canonical; the forge is a projection and never flows back as authority.",
  "scripts": {
    "proof": "tsx src/proof.ts"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
