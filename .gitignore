# dependencies & build output
node_modules
dist/
.vite/

# SpexCode worktrees live here — they are separate checkouts, never committed
.worktrees/

# per-worktree session linker — ephemeral runtime state, not spec data
.session
# originating-prompt sidecar (multi-line launch request) — runtime metadata, not spec data
.session-prompt
# deferred launch prompt for a still-QUEUED session (consumed at launch) — runtime, not spec data
.session-launch
# per-worktree generated hook settings (writeSettings() writes absolute paths) — runtime, not spec data
.spex-hooks.json
# per-worktree generated launch script (launchScript() writes the claude invocation) — runtime, not spec data
.spex-launch.sh
# per-worktree hidden CLAUDE.md copy (hideClaudeMd renames CLAUDE.md so Claude Code won't auto-load it) — runtime, not spec data
CLAUDE.spexhidden.md

# harness state
.claude/

# machine-local claude binary symlink (points into a per-machine install); the session
# launcher resolves `claude` from PATH (SPEXCODE_CLAUDE_CMD), so this is never vendored
spec-cli/bin/claude

# os / editor
.DS_Store
*.log
